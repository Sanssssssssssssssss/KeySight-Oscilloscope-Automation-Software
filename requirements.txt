# GUI runtime notes:
# - tkinter ships with standard Python on Windows and is not installed via pip.
# - A VISA implementation such as NI-VISA or Keysight IO Libraries Suite
#   is still required for hardware communication.

matplotlib>=3.8
numpy>=1.26
openpyxl>=3.1
pandas>=2.2
Pillow>=10.0
PyVISA>=1.14
PySide6>=6.8
